---
title: "Regex-strip Example"
filters:
  - regex-strip
regex-strip-pattern:
  - "#%s?nolint.*"
---

The below is `ABC <- 1 # nolint` but `# nolint` part had been removed.
```{r}
ABC <- 1 # nolint
```
