```{r save testenv, include=FALSE, opts.label='include_if_testing'}
#| renv.ignore: TRUE
if(exists("tenv")) {
  dirname <- Sys.getenv("QUARTO_TESTTHAT_DATA_PATH")
  filename <- paste0(gsub("\\.rmarkdown$", "", knitr::current_input()), ".rds")
  filepath <- file.path(dirname, filename)
  file.create(filepath)
  saveRDS(tenv, filepath)
}
```
